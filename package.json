{
  "name": "collaborative-canvas",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time collaborative drawing canvas — vanilla JavaScript + HTML5 Canvas + WebSockets",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "dependencies": {
    "ws": "^8.17.1"
  }
}
